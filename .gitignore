# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-analyze
dist-ssr
*.local

# Editor directories and files. Recommended extensions and the run targets are
# shared on purpose; everything else under .vscode is personal.
.vscode/*
!.vscode/extensions.json
!.vscode/tasks.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sw?

# The backend has its own rules for build output, local secrets and IDE state.
# Nothing here may ignore a solution file: backend/TeamProgressTracker.slnx is
# needed to build, which is why the Vite template's *.sln line is gone.

